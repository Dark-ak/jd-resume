import type { ProviderGenerateArgs } from "@/lib/aiProvider";

export async function generateWithMuseSpark(
  _args: ProviderGenerateArgs,
): Promise<string> {
  // TODO(you): implement the Muse Spark call here and return the raw JSON string.
  throw new Error(
    "Muse Spark is not wired up yet — implement generateWithMuseSpark() in lib/providers/museSparkProvider.ts.",
  );
}
